var Handler = require("Handler");
var TimerManager = require("TimerManager");

var UIComm_EnterCancelTips = cc.Class({
        extends:cc.Component,

        properties: () =>({

        }),

        move(dir, type, startPos, moveYDic, time, standTime = 5, callBack = null){

        }
})